# MCP Server Configuration
# Copy this file to .env and fill in your values

# Server settings
MCP_API_KEY=your-secret-api-key-here
MCP_PORT=8765
MCP_HOST=127.0.0.1

# Filesystem security - all file operations restricted to this directory
# Use absolute path. Default: user home directory
MCP_BASE_DIR=~

# Enable/disable toolsets (comma-separated)
# Available: system, gmail, discord
# Example: system,gmail,discord
MCP_ENABLED_TOOLSETS=system

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# ============ Gmail Setup ============
# 1. Create a project at https://console.cloud.google.com/
# 2. Enable the Gmail API
# 3. Create OAuth 2.0 credentials (Desktop app)
# 4. Download as 'credentials.json' in this directory
# 5. Add 'gmail' to MCP_ENABLED_TOOLSETS above
# 6. On first Gmail tool use, browser will open for OAuth

# ============ Discord Setup ============
# 1. Go to https://discord.com/developers/applications
# 2. Create application and add a bot
# 3. Enable Message Content Intent and Server Members Intent
# 4. Copy bot token and add below
# 5. Invite bot to your server (OAuth2 URL Generator)
# 6. Add 'discord' to MCP_ENABLED_TOOLSETS above

DISCORD_BOT_TOKEN=

# Notion Integration
NOTION_API_KEY=
